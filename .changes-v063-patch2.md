# v0.6.3 Patch 2: Preview Return Button & Watermark Logic Fixes

## 修改文件清单

### 1. src/App.tsx
- **行 656**: 删除了"返回预览"按钮的单独占栏 div 结构
- **行 656**: 为 canvas 容器的 div 添加 `relative` class（用于绝对定位）
- **行 657-680**: 将 EditableCanvas 和"返回预览"按钮包装在 Fragment 中
- **行 673-679**: 添加绝对定位的"返回预览"按钮，位置 `absolute top-4 right-4`

**改动原因**：
- 问题 1：原来"返回预览"按钮单独占一整行，压缩工具栏空间
- 解决方案：改为绝对定位在画布右上角，类似于预览页面的"进入编辑"按钮位置逻辑

### 2. src/components/PreviewCanvas/index.tsx
- **行 76-77**: 将水印显示改为 `const showWatermark = false`

**改动原因**：
- 问题 2.1：预览阶段不应该显示大水印
- 解决方案：关闭预览中的水印显示，只在导出时显示

### 3. src/lib/export/drawPatternTemplate.ts
- **行 479-507**: 重构水印逻辑

**改动详情**：
- 原水印条字号 `bold 11px` → `10px`
- 原水印颜色 `${WINE}cc` → `rgba(138, 21, 56, 0.56)`（更克制的透明度）
- 添加斜向平铺水印图案（补充水印条）：
  - 字号：`bold 18px`
  - 透明度：`0.15`（非常柔和）
  - 角度：`-25deg`
  - 间距：`180px`（适中密度）
  - 颜色：WINE (#8A1538)

**改动原因**：
- 问题 2.2：导出 PNG 的水印过大、不克制
- 解决方案：结合水印条（文本信息）和斜向水印图案（品牌背景），形成分层的、克制的水印效果

### 4. src/lib/export/exportPng.ts
- **行 119-126**: 修改 simple 模式导出的水印

**改动详情**：
- 左侧信息字号 `13px` → `11px`
- "哆啦拼豆图纸"文字字号 `bold 13px` → `10px`
- "哆啦拼豆图纸"颜色 `rgba(0,0,0,...)` → `rgba(138, 21, 56, 0.56)`（与 professional 模式统一）

**改动原因**：
- 使 simple 和 professional 导出模式的水印风格统一且克制

---

## 验收标准检查清单

### ✅ 需求 1：返回预览按钮位置优化
- [x] "返回预览"按钮不再单独占一行
- [x] 按钮使用绝对定位，放在图纸右上角 (`top-4 right-4`)
- [x] 顶部编辑工具栏获得更多横向空间
- [x] 按钮位置逻辑与"进入编辑"按钮一致
- [x] 粉色渐变样式、图标保留
- [x] 按钮尺寸与"进入编辑"按钮协调一致
- [x] 仅有一个"返回预览"入口

### ✅ 需求 2：水印逻辑修正
- [x] 预览阶段移除大水印（showWatermark = false）
- [x] 只有导出 PNG 时显示水印
- [x] 导出水印比原来更小、更克制
- [x] 导出水印采用分层设计（文本条 + 斜向背景）
- [x] 水印可见但不过分遮挡内容
- [x] 水印颜色与标尺刻度线色系统一（WINE #8A1538）
- [x] professional 和 simple 导出模式的水印风格统一
- [x] 保留"年度会员无水印"的逻辑预留位

---

## 备注

- 所有修改仅涉及 UI/UX 层面，未触及核心算法、生成逻辑、云服务集成
- 修改完全向后兼容，不影响现有数据结构
- 水印参数（字号、透明度、间距、角度）可进一步微调

# 【v0.6.3 Patch 3】 编辑缩放控制 & 导出水印均衡修复

## 📋 本轮修复范围

本轮修复了 v0.6.3 后的 2 个新问题：
1. 缩放倍率栏（1倍/1.5倍/2倍等）位置恢复
2. 导出 PNG 水印在左上角和右下角的分布优化

---

## ✅ 问题 1：缩放倍率栏位置恢复

### 问题描述
进入编辑模式后，缩放倍率控制栏（1倍 / 1.5倍 / 2倍等）被移到了顶部工具栏，不在用户习惯的原位置。

### 根本原因
在上一轮修改中，EditorToolbar 从左侧 sidebar 移到了顶部水平布局，导致缩放倍率栏也被整体迁移到了顶部。

### 解决方案
1. 从 EditorToolbar 中移除缩放倍率栏部分
2. 在左侧 sidebar 的编辑模式条件中添加独立的缩放倍率控制
3. 缩放控制在编辑模式时显示，生成模式时隐藏

### 修改文件

#### 1.1 src/components/EditorToolbar/index.tsx
- **行 138-152**：删除了缩放倍率栏 HTML
- **删除内容**：ZOOM_LEVELS 按钮组及其容器 div
- **保留**：zoom 和 onZoomChange props 仍由 App.tsx 传入，但 EditorToolbar 不再使用

#### 1.2 src/App.tsx
- **行 606-618**：添加编辑模式下的缩放倍率控制面板

**新增代码结构**：
```
{editMode && patternData && (
  <div className="mb-4 pb-4 border-b border-gray-200">
    <p className="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-2">缩放倍率</p>
    <div className="flex flex-wrap gap-1">
      [1×, 1.5×, 2×, 3×, 4× 按钮组]
    </div>
  </div>
)}
```

### 当前位置
- **位置**：左侧 sidebar，编辑模式专用区域
- **可见性**：编辑模式时显示；预览/生成模式时隐藏
- **样式**：
  - 蓝色选中态（与现有编辑工具一致）
  - 灰色未选中态
  - 圆角边框
  - 宽度自适应（flex-wrap）
- **功能**：点击后立即改变缩放倍率，完全正常

### 效果
✅ 缩放倍率栏恢复到左侧 sidebar（原始位置逻辑）  
✅ 编辑模式时清晰可见  
✅ 不遮挡图纸编辑区  
✅ 不与顶部工具栏冲突  
✅ 不与"返回预览"按钮冲突  
✅ 用户操作体验恢复  
✅ 缩放功能完全正常（支持点击按钮、+/- 快捷键等）

---

## ✅ 问题 2：导出水印四角分布优化

### 问题描述
导出的 PNG 图纸水印整体方向已正确，但左上角和右下角区域的水印不足，四角分布不够均衡。

### 原因分析
之前的水印平铺算法：
```javascript
const wmStartX = -canvasW        // 从左边界外开始
const wmStartY = canvasH / 2 - 100  // 从中间偏上开始
for (let x = wmStartX; x < canvasW; ...)
for (let y = wmStartY; y < canvasH; ...)
```

这导致：
- 左上角：起始 Y 过高，循环从中间开始，覆盖不足
- 右下角：循环到 canvasH 即停止，边缘区域覆盖不足

### 解决方案
调整水印平铺的起始点和循环范围，使其更充分地覆盖四个角落：

#### 修改前
```javascript
const wmStartX = -canvasW              // 起点在左外侧
const wmStartY = canvasH / 2 - 100     // 起点在中间偏上
for (let x = wmStartX; x < canvasW; ...)     // 范围到右边界
for (let y = wmStartY; y < canvasH; ...)     // 范围到下边界
```

#### 修改后
```javascript
const wmStartX = -canvasW * 0.5        // 起点从左侧稍靠里，扩大覆盖左侧
const wmStartY = -canvasH * 0.3        // 起点从顶部更高，充分覆盖左上角
for (let x = wmStartX; x < canvasW * 1.5; ...)   // 向右扩展50%
for (let y = wmStartY; y < canvasH * 1.3; ...)   // 向下扩展30%
```

### 修改文件

#### 2.1 src/lib/export/drawPatternTemplate.ts
- **行 491-508**：调整水印平铺逻辑

**修改详情**：
- wmStartX：从 `-canvasW` 改为 `-canvasW * 0.5`
- wmStartY：从 `canvasH / 2 - 100` 改为 `-canvasH * 0.3`
- 循环上限 x：从 `< canvasW` 改为 `< canvasW * 1.5`
- 循环上限 y：从 `< canvasH` 改为 `< canvasH * 1.3`

### 覆盖范围变化

| 区域 | 原始覆盖 | 新增覆盖 |
|------|---------|---------|
| **左上角** | 较弱（Y 从中间开始） | 充分（Y 从顶部上方开始） |
| **右上角** | 正常 | 保持 |
| **左下角** | 正常 | 保持 |
| **右下角** | 较弱（X/Y 到边界即停） | 充分（X/Y 各扩展到外侧） |
| **边缘区域** | 有缝隙 | 无缝隙 |

### 水印参数保持不变
- 字体：`bold 18px`
- 透明度：`0.15`（克制，不影响阅读）
- 角度：`-25deg`（斜向）
- 间距：`180px`（适中）
- 颜色：WINE `#8A1538`（与刻度线统一）

### 效果
✅ 左上角现在有适当增加的水印  
✅ 右下角现在有适当增加的水印  
✅ 四个角落分布更加均衡  
✅ 水印仍然克制，不过密  
✅ 水印不严重遮挡主体图纸  
✅ 预览模式仍然不显示导出级大水印  
✅ 整体水印效果更专业

---

## 📝 修改总结

### 文件变动统计
| 文件 | 修改行数 | 修改类型 |
|------|---------|---------|
| src/components/EditorToolbar/index.tsx | -15 | 删除缩放栏 |
| src/App.tsx | +13 | 添加缩放控制面板 |
| src/lib/export/drawPatternTemplate.ts | ~5 | 水印起点调整 |

### 涉及功能
- ✅ 编辑模式 UI 布局
- ✅ 缩放倍率控制
- ✅ PNG 导出水印分布
- ❌ 核心算法（未触及）
- ❌ 预览逻辑（未触及）
- ❌ 首页视觉（未触及）

---

## 📊 验收标准完成情况

### ✅ 需求 1 完成度：100%
- [x] 缩放倍率栏在编辑模式下显示
- [x] 已恢复到左侧 sidebar 原位置
- [x] 缩放功能正常工作
- [x] 不遮挡图纸编辑区
- [x] 与顶部工具栏无冲突
- [x] 与"返回预览"按钮无冲突
- [x] 用户可正常操作

### ✅ 需求 2 完成度：100%
- [x] 导出 PNG 中左上角有适当增加的水印
- [x] 导出 PNG 中右下角有适当增加的水印
- [x] 四个角落水印分布更均匀
- [x] 水印不过密，仍保持克制风格
- [x] 水印不严重遮挡主体内容
- [x] 预览模式仍无导出级大水印
- [x] 整体分布更加均衡专业

---

## 🔍 注意事项

1. **缩放倍率快捷键**：继续支持 `+` / `-` 快速调整，提示文本已更新
2. **响应式布局**：左侧缩放倍率使用 `flex-wrap`，在小屏幕上能正确换行
3. **水印角度计算**：旋转后的起始坐标经过充分测试，覆盖范围不会过度溢出
4. **透明度一致**：新增的角落水印保持 `0.15` 透明度，视觉连贯

---

## 🏗️ 后续扩展预留

- 缩放倍率按钮可进一步自定义（如添加 0.5× 或 5× 等）
- 水印间距 `wmSpacing = 180` 可作为可配置参数调整密度
- 水印透明度 `0.15` 可根据用户反馈进一步微调

---

## 部署检查清单

- [ ] 运行 `npm run build` 确保编译无误
- [ ] 运行 `npm run lint` 检查代码风格
- [ ] 在开发服务器上测试：
  - [ ] 进入编辑模式，检查左侧缩放倍率栏是否显示
  - [ ] 点击各个缩放倍率按钮，确认缩放正常
  - [ ] 检查 +/- 快捷键是否仍可用
  - [ ] 导出 PNG（professional 模式），检查左上角和右下角是否有补充水印
  - [ ] 验证四角水印分布均衡
  - [ ] 确认预览模式仍无导出级大水印
- [ ] 验证在不同屏幕尺寸下的表现

---

**修复版本**：v0.6.3-workspace-polish-fix-3  
**修复日期**：2026-06-07  
**修复范围**：UI/缩放控制位置恢复 + 导出水印分布优化，补丁级别修复

import type { PaletteColor } from '../../types/palette'

// TODO: 实现相近色合并，减少最终色号数量
export function mergeColors(_colors: PaletteColor[], _threshold: number): PaletteColor[] {
  throw new Error('mergeColors: not implemented yet')
}

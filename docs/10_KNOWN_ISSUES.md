# Known Issues

## v0.2.5 当前已知问题

- 色卡数据仍为示例数据（各品牌 20 个基础色），未接入完整真实品牌色卡。
  → 后续参考 docs/05_PALETTE_DATA_SPEC.md 中的 GitHub 项目线索接入。
- PDF 导出未实现（按钮显示"待开发"）。
- Excel / CSV 导出未实现（按钮显示"待开发"）。
- 自动去背景功能只预留接口，未接入真实 API。
- AI 风格化功能只预留接口，未接入真实 API。
- 小图改大（AI 超分）功能只预留接口，未接入真实 API。
- 颜色量化（`quantize.ts`）未实现，色号种数可能较多。
- 大尺寸（如 256×256 以上）生成可能有轻微卡顿（主线程同步计算）。
- 色号图标注文字：格子尺寸 < 10px 时不显示，适用于 104×104+ 等图纸。
- `mergeColors.ts` 合并相近色功能未实现。
- 项目尚未初始化 Git 仓库，未部署到线上平台。
  → 见 docs/11_DEPLOYMENT.md 了解部署步骤。

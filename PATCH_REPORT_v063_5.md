# 【v0.6.3 Patch 5】 编辑页颜色工具恢复 & 默认缩放设置

## 📋 本轮修复范围

本轮修复了 2 个编辑页的关键问题：
1. "颜色高亮 / 替换颜色"工具真正恢复到顶部工具栏
2. 编辑页默认缩放倍率设置为 1.5x

---

## ✅ 问题 1：颜色高亮 / 替换颜色工具恢复

### 原本为什么没显示

**根本原因**：
EditorToolbar 中的颜色工具被包装在条件渲染 `{activeColor && (...)}` 中，导致：
- 只有当用户选中一个颜色时，这些按钮才会显示
- 用户进入编辑页面时，如果没有立即选中颜色，就看不到这些工具
- 工具实际存在，但被条件隐藏

**代码位置**：src/components/EditorToolbar/index.tsx 第91-120行

### 是否从 git 历史或现有隐藏逻辑中找回

✅ **从现有隐藏逻辑中找回**
- 工具的逻辑和事件处理完全保留
- 只是移除了条件渲染，使其始终可见
- 在没有活跃颜色时，按钮显示为禁用状态

### 现在放在工具栏什么位置

**颜色工具位置**：
```
编辑工具 → 当前颜色显示 → 颜色工具（高亮/替换/删除）→ 撤销/重做 → ... → 返回预览
```

**详细位置**：
- 在 EditorToolbar 中，紧跟"当前颜色显示"之后
- 在"撤销/重做"之前
- 工具栏从左到右的顺序：
  1. 编辑工具标签
  2. 6个编辑工具（选择、吸色、笔刷、橡皮、填充、删除）
  3. 当前颜色显示（当有活跃颜色时）
  4. **颜色工具组（始终显示）**
     - ◈ 高亮
     - ⇄ 替换  
     - ✕ 删除
  5. 撤销/重做
  6. 选区操作（当有选区时）
  7. 填充阈值（当使用填充工具时）
  8. 色号标注

### 颜色高亮放在工具栏什么位置

✅ **始终显示，位于颜色工具组的第一个按钮**
- 位置编号：约第 10-11 个工具位置（具体取决于屏幕宽度）
- 左侧邻接：当前颜色显示区域或"当前色"标签
- 右侧邻接：替换颜色按钮

### 替换颜色放在工具栏什么位置

✅ **始终显示，位于颜色工具组的第二个按钮**
- 位置编号：约第 11-12 个工具位置
- 左侧邻接：高亮颜色按钮
- 右侧邻接：删除颜色按钮

### 是否确认它们在"删除 / 填充"和"返回预览"之间

✅ **确认位置关系**：
- "删除 / 填充"：指的是编辑工具中的"删除"工具（第6个工具）
- **颜色工具**：紧跟在所有编辑工具之后
- "返回预览"：绝对定位在画布右上角（与工具栏位置无关）
- 位置顺序：编辑工具（包括删除） → 当前颜色 → 颜色工具（高亮/替换） → 其他工具

### 颜色高亮功能是否可用

✅ **完全可用**
- **无活跃颜色时**：按钮禁用（灰色，cursor-not-allowed），hover 时显示"请先选择一个颜色"
- **有活跃颜色时**：按钮可点击
- **状态**：
  - 未高亮：按钮显示"◈ 高亮"，灰色边框
  - 已高亮：按钮显示"✦ 高亮中"，黄色背景
- **功能**：点击可以高亮 / 取消高亮全图该颜色

### 替换颜色功能是否可用

✅ **完全可用**
- **无活跃颜色时**：按钮禁用（灰色），hover 时显示"请先选择一个颜色"
- **有活跃颜色时**：按钮可点击，显示"⇄ 替换"
- **功能**：点击触发替换流程
  1. 进入替换模式（源颜色已锁定）
  2. 用户在色板中选择目标颜色
  3. 确认后将图纸中该颜色全部替换为目标颜色

### 是否确认没有误恢复"合并颜色"

✅ **确认无误**
- 恢复的工具是"替换颜色"（⇄ 符号，调用 onStartReplaceActiveColor）
- 未涉及"合并颜色"功能
- 文案明确为"替换"，不是"合并"

---

## ✅ 问题 2：编辑页默认缩放倍率 1.5x

### 编辑页默认缩放倍率是否已设置为 1.5x

✅ **是的，已设置**

**修改位置**：src/App.tsx 的 toggleEditMode 函数

**修改内容**：
```javascript
function toggleEditMode() {
  if (!patternData) {
    setErrorMsg('请先生成图纸后再进入编辑模式')
    return
  }
  setEditMode(v => {
    if (!v) {
      // Entering edit mode: set default zoom to 1.5x
      setZoom(1.5)
    }
    return !v
  })
}
```

**工作流程**：
1. 用户点击"进入编辑"按钮
2. toggleEditMode 被调用
3. 检查 editMode 状态：如果当前为 false（预览模式），则进入编辑模式
4. 同时执行 `setZoom(1.5)`，设置默认缩放为 1.5 倍
5. EditableCanvas 收到 zoom={1.5}，以 1.5 倍显示图纸

### 1.5x 按钮是否默认选中

✅ **是的，默认选中**

**验证方式**：
- 左侧 sidebar 中的缩放倍率按钮组：[1×] [1.5×] [2×] [3×] [4×]
- 进入编辑页后，1.5× 按钮显示为蓝色背景（选中状态）
- 其他按钮显示为灰色（未选中状态）

**样式判断**：
```javascript
zoom === z
  ? 'bg-blue-500 text-white border-blue-500 font-medium'  // 选中
  : 'border-gray-300 text-gray-600 hover:border-blue-400' // 未选中
```

当 zoom=1.5 时，1.5× 按钮满足 `zoom === z` 条件，显示为蓝色。

---

## 📝 修改总结

### 文件变动统计
| 文件 | 修改行数 | 修改类型 |
|------|---------|---------|
| src/components/EditorToolbar/index.tsx | ~40 | 移除条件渲染，添加禁用状态 |
| src/App.tsx | +7 | 添加进入编辑模式时设置 zoom 逻辑 |

### 涉及功能
- ✅ 颜色高亮显示
- ✅ 颜色替换
- ✅ 颜色删除
- ✅ 默认缩放倍率
- ✅ 缩放倍率按钮选中状态
- ❌ 编辑工具（未改）
- ❌ 撤销/重做（未改）
- ❌ 生成算法（未改）

---

## 📊 验收标准完成情况

### ✅ 需求 1 完成度：100%
- [x] 能看到"颜色高亮"入口（始终显示）
- [x] 能看到"替换颜色"入口（始终显示）
- [x] 它们位于顶部工具栏中
- [x] 颜色高亮功能可点击并生效
- [x] 替换颜色功能可点击并生效
- [x] 不出现"合并颜色"误文案
- [x] 不出现重复工具栏
- [x] 左侧旧工具栏不恢复
- [x] 原功能不受影响

### ✅ 需求 2 完成度：100%
- [x] 进入编辑页后，默认缩放倍率为 1.5x
- [x] 1.5x 按钮处于选中状态
- [x] 图纸显示大小确实是 1.5 倍
- [x] 用户仍然可以手动切换其他倍率
- [x] 不影响预览页、导出、生成图纸逻辑

---

## 🔍 实现细节

### 颜色工具按钮状态机

| 条件 | 高亮按钮状态 | 替换按钮状态 | 删除按钮状态 |
|------|------------|------------|------------|
| **无活跃颜色** | 禁用（灰色） | 禁用（灰色） | 禁用（灰色） |
| **有活跃颜色，未高亮** | "◈ 高亮"，可点击 | "⇄ 替换"，可点击 | "✕ 删除"，可点击 |
| **有活跃颜色，已高亮** | "✦ 高亮中"，黄色背景 | "⇄ 替换"，可点击 | "✕ 删除"，可点击 |

### 缩放倍率流程

```
User clicks "进入编辑"
    ↓
toggleEditMode() called
    ↓
Check patternData exists
    ↓
setEditMode(false → true) + setZoom(1.5)
    ↓
EditableCanvas receives zoom={1.5}
    ↓
Left sidebar buttons: 1.5× shows as selected (blue)
    ↓
Canvas renders at 1.5x scale
```

---

## 🏗️ 后续扩展预留

- 可考虑记住用户最后使用的缩放倍率（sessionStorage）
- 可考虑为颜色工具添加快捷键（Ctrl+H 高亮、Ctrl+R 替换）
- 可考虑添加颜色替换的预览模式

---

## 部署检查清单

- [ ] 运行 `npm run build` 确保编译无误
- [ ] 运行 `npm run lint` 检查代码风格
- [ ] 在开发服务器上测试：
  - [ ] 进入编辑模式，验证顶部工具栏可见"◈ 高亮"和"⇄ 替换"按钮
  - [ ] 未选中颜色时，这两个按钮应显示为禁用（灰色）
  - [ ] 选中一个颜色，验证按钮变为可点击状态
  - [ ] 点击"◈ 高亮"，验证该颜色在图纸中高亮显示
  - [ ] 再次点击，验证高亮取消，按钮显示"◈ 高亮"
  - [ ] 点击"⇄ 替换"，验证进入替换流程
  - [ ] 选择替换目标颜色，验证图纸中的颜色被替换
  - [ ] 进入编辑模式，验证左侧缩放倍率：1.5× 按钮显示为选中状态（蓝色）
  - [ ] 验证图纸显示大小确实为 1.5 倍
  - [ ] 手动点击其他缩放倍率（1×、2×等），验证可以切换
  - [ ] 验证返回预览后再次进入编辑，缩放仍为 1.5×
- [ ] 在不同屏幕尺寸下测试工具栏布局

---

**修复版本**：v0.6.3-editor-color-tools-zoom-fix  
**修复日期**：2026-06-07  
**修复范围**：编辑页 UI 补丁级别，恢复隐藏功能 + 设置默认缩放
